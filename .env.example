# 访问密码：设置后打开网站需先登录（不设置则完全公开）
AUTH_PASSCODE=

# 可选：Vercel Cron 调用 /api/cron 时校验的密钥（部署到 Vercel 后自动生成，一般无需手动填）
CRON_SECRET=

# 数据库连接串（本地开发用；Vercel 上由 Postgres 存储自动注入，无需配置）
# DATABASE_URL=postgres://user:password@host:5432/dbname
